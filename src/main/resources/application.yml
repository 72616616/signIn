mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml

# 端口号
server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:/xxxxx/signin?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: xxxx
    password: xxxx
  thymeleaf:
    prefix: classpath:/public/